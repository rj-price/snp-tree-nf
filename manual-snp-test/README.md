# SNP Pipeline

The script assumes files follow a standard naming convention (e.g., Sample1_R1_001.fastq.gz). It extracts the SAMPLE_NAME to ensure output files are named cleanly.

## Set up environment
```bash
conda env create -f environment.yaml
```
